# Dependencies
node_modules/

# Local secrets — never commit
config/accounts.local.json
config/card.local.json
.env
.env.local
*.pem

# Ledger / runtime data — `data/` is committed (viewer page + ledger.json live there).
# Add explicit ignores below if some files in `data/` should stay local-only.
*.db

# Playwright
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Build / OS
dist/
.DS_Store
*.log

# Brainstorming companion (transient mockups)
.superpowers/
