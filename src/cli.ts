#!/usr/bin/env node
/**
 * CLI entry — subcommands added in later phases (config check, run, ledger).
 */
console.error("tennis-booking: use `npm run cli -- --help` once commands land. Phase 1: planner is library + tests only.");
